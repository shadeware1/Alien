s_ 

[3.5] | {
       $get_meta.pkg {
            see.sig(extMint-int) {
                    see sig(dst) = create_(inp/opt) in (local-frame) cache.config(-lcs:bin) cmd_log 
                    dta keystroke(dst) bin:txt
            }
       }
}


